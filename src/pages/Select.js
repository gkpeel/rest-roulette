import React from 'react';
import { Grid } from 'semantic-ui-react';

const Select = props => {
	return (
		<Grid container columns={1}>
			<Grid.Row>
				<Grid.Column>
					<h1>Let's Pick a Place</h1>
				</Grid.Column>
			</Grid.Row>
		</Grid>
	);
};

export default Select;
